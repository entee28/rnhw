export interface ITheme {
  background: string;
  text: string;
}

// Light theme colors
export const lightColors: ITheme = {
  background: '#FFFFFF',
  text: '#121212',
};

// Dark theme colors
export const darkColors: ITheme = {
  background: '#121212',
  text: '#FFFFFF',
};
